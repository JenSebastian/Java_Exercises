package com.siriuscom.shapes;

import java.util.ArrayList;
import java.util.Collection;
import java.util.List;

//public class Shape_ <Object>{ 
//	
//	public void calculateArea(Object t) {
//		if (t instanceof String) {
//			String str = (String)t; //typecasting on t cuz we are not using generics. if we are using generics we dont need to typecast
//		}
//	}
//
//}

//public class ShapeWithMethod<T> {
//	private T object;
//	public void setObject(T t) {
//		this.object = t;
//	}
//	public <A> void getNameOfClass(A)
//}




public class ShapeWithMultiple <T> {
	/*
	public int countElements(Collection<T> items) {
	int count = 0;
			for (T item : items) {
				count++;
			}
	return count;
	}
}

public <T extends Number & List> int countElementsIfDivBy3(Collection<T> items, int value) {   //can extend 2, first has to be class, second can be interface..can handle 
	int count = 0;
			for (T item : items) {
				if(item.intValue() % value == 0);
				count++;
			}
	return count;
}

public void main(String[] args) {
	ShapeWithMultiple<Integer> shape = new ShapeWithMultiple<Integer>();
	List<Integer> list = new ArrayList<Integer>();
	shape.countElementsIfDivBy3(list, 3);

*/
	}

